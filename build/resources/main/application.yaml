spring:
  application:
    name: nyayassist

  main:
    web-application-type: none
    banner-mode: off

  ai:
    mcp:
      server:
        name: lawyer-assistant
        version: 1.0.0
        transport: STDIO
        type: SYNC

logging:
  level:
    root: OFF
  file:
    name: /tmp/nyayassist.log