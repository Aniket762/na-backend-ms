spring:
  application:
    name: "nyayassist"
  main:
    banner-mode: "off"
  ai:
    mcp:
      server:
        type: SYNC
        name: "LawyerScraperServer"
        version: "1.0.0"

server:
  port: 8080

logging:
  pattern:
    console: ""
  file:
    name: "mcp-server.log"